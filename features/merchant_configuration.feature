# language: zh-CN
功能: 商户首次接入配置
  商户以 appKey、套件与密钥对装配 SDK 客户端（spec F1、Q7、§2）。
  配置错误应在装配期立即给出明确错误，而非等到请求期。

  场景: 用 RSA3072 套件装配客户端
    假如 商户选择套件 "WOP-RSA3072-SHA256"
    那么 客户端装配成功
    而且 套件密钥长度为 3072 位
    而且 套件摘要标签为 "sha-256"
    而且 套件 bulk 算法为 "AES-256-GCM"

  场景: SM2-SM3 套件按路线图明确拒绝
    假如 商户选择套件 "WOP-SM2-SM3"
    那么 客户端装配失败
    而且 错误文案包含 "暂未支持"

  场景: 国际与国密跨族组合被拒绝
    假如 商户选择套件 "WOP-RSA3072-SM3"
    那么 客户端装配失败
    而且 错误文案包含 "不支持的算法组合"

  场景: 格式非法的套件串被拒绝
    假如 商户选择套件 "RSA3072"
    那么 客户端装配失败
    而且 错误文案包含 "格式错误"
